extends include/layout.pug
include include/mixins.pug
block append properties
    -
        var title = "SmarkForm Empty Template";
block mainForm
    section
        div Do your magic here... 😉
    section
        .form-group
            .spacer
            button(data-smark = {
                action: "empty",
            }, title="Clear form data") ❌ Cancel
            button(data-smark = {
                action: "export",
            }, title="Submit form data") 💾 Submit
